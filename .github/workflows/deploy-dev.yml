name: Deploy Dev

on:
  push:
    branches:
      - develop
    paths:
      - "services/**"
      - "deploy/**"
      - ".github/workflows/deploy-dev.yml"

  workflow_dispatch:
    inputs:
      service:
        description: "Specific service to deploy (optional)"
        required: false
        type: string

permissions:
  contents: read
  deployments: write
  checks: write
  id-token: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  ENVIRONMENT: dev
  REGISTRY: ghcr.io/${{ github.repository_owner }}

jobs:

  detect-services:
    name: Detect Services
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.matrix.outputs.matrix }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build Matrix
        id: matrix
        shell: bash
        run: |
          if [ -n "${{ github.event.inputs.service }}" ]; then
            echo 'matrix=["${{ github.event.inputs.service }}"]' >> $GITHUB_OUTPUT
            exit 0
          fi

          CHANGED=$(git diff HEAD~1 HEAD --name-only | grep '^services/' | cut -d/ -f2 | sort -u || true)

          if [ -z "$CHANGED" ]; then
            CHANGED=$(find services -mindepth 1 -maxdepth 1 -type d -exec basename {} \;)
          fi

          JSON=$(printf '%s\n' "$CHANGED" | jq -R -s -c 'split("\n")[:-1]')
          echo "matrix=$JSON" >> $GITHUB_OUTPUT

  build-and-push:
    name: Build (${{ matrix.service }})
    runs-on: ubuntu-latest
    needs: detect-services

    strategy:
      fail-fast: false
      matrix:
        service: ${{ fromJson(needs.detect-services.outputs.matrix) }}

    outputs:
      image_ref: ${{ steps.digest.outputs.image_ref }}

    steps:
      - uses: actions/checkout@v4

      - name: Verify Dockerfile
        run: test -f services/${{ matrix.service }}/Dockerfile

      - name: Docker Login
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Push
        id: build
        uses: docker/build-push-action@v5
        with:
          context: services/${{ matrix.service }}
          push: true
          tags: ${{ env.REGISTRY }}/${{ matrix.service }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Capture Digest
        id: digest
        run: |
          echo "image_ref=${{ env.REGISTRY }}/${{ matrix.service }}@${{ steps.build.outputs.digest }}" >> $GITHUB_OUTPUT

  deploy:
    name: Deploy (${{ matrix.service }})
    runs-on: ubuntu-latest
    needs:
      - detect-services
      - build-and-push

    strategy:
      fail-fast: false
      matrix:
        service: ${{ fromJson(needs.detect-services.outputs.matrix) }}

    environment:
      name: dev

    steps:
      - uses: actions/checkout@v4

      - name: Configure Cloud Auth (OIDC Placeholder)
        run: echo "Configure cloud federation here"

      - name: Detect Deployment Target
        id: target
        shell: bash
        run: |
          if [ -f "services/${{ matrix.service }}/service.yml" ]; then
            TYPE=$(grep '^target:' services/${{ matrix.service }}/service.yml | awk '{print $2}')
            echo "type=$TYPE" >> $GITHUB_OUTPUT
          elif [ -d deploy/k8s ]; then
            echo "type=k8s" >> $GITHUB_OUTPUT
          elif [ -d deploy/ecs ]; then
            echo "type=ecs" >> $GITHUB_OUTPUT
          else
            echo "No deployment target"
            exit 1
          fi

      - name: Deploy Generic
        run: |
          chmod +x deploy/scripts/deploy.sh
          deploy/scripts/deploy.sh \
            "${{ matrix.service }}" \
            "${{ env.ENVIRONMENT }}" \
            "${{ needs.build-and-push.outputs.image_ref }}" \
            "${{ steps.target.outputs.type }}"

      - name: Health Check
        shell: bash
        run: |
          if [ -f "services/${{ matrix.service }}/service.yml" ]; then
            PATH_CHECK=$(grep '^healthcheck:' services/${{ matrix.service }}/service.yml | awk '{print $2}')
          else
            PATH_CHECK="/health"
          fi

          chmod +x ci/smoke-test.sh
          ci/smoke-test.sh "${{ matrix.service }}" "${PATH_CHECK}" "${{ env.ENVIRONMENT }}"

      - name: Auto Rollback on Failure
        if: failure()
        run: |
          chmod +x deploy/scripts/rollback.sh
          deploy/scripts/rollback.sh \
            "${{ matrix.service }}" \
            "${{ env.ENVIRONMENT }}" \
            "${{ steps.target.outputs.type }}"

  summary:
    name: Deployment Summary
    if: always()
    runs-on: ubuntu-latest
    needs:
      - build-and-push
      - deploy

    steps:
      - name: Publish Summary
        run: |
          echo "## Dev Deployment Completed" >> $GITHUB_STEP_SUMMARY
          echo "- Immutable images built" >> $GITHUB_STEP_SUMMARY
          echo "- Services deployed to dev" >> $GITHUB_STEP_SUMMARY
          echo "- Health checks executed" >> $GITHUB_STEP_SUMMARY
          echo "- Auto rollback enabled on failure" >> $GITHUB_STEP_SUMMARY
